name: Reveal Secret
on: workflow_dispatch
jobs:
  reveal:
    runs-on: ubuntu-latest
    steps:
      - name: Reveal
        env:
          SECRET: ${{ secrets.CONFIG_PASSPHRASE }}
        run: |
          echo -n "$SECRET" | sed 's/./& /g'
